{
  "name": "ontaic-dashboard-example",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "compile": "python -m ontaic.compiler --input app.py --output src/schema.json"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
